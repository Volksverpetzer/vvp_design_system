---
"@volksverpetzer/ui-web": minor
---

Add a `size` prop to Badge (`"sm" | "md"`, default `"sm"`). `md` matches vvp_divi5_extensions's category badges (Prüfpunkt, YouTube, Instagram, Podcast tags), which use a larger `--vvp-font-size-base` (16px) label instead of the compact `--vvp-font-size-xs` (12px) default.
