---
"@volksverpetzer/ui-web": minor
---

Move `ToolPage`'s `tags` from directly under the title to below `children`, so they read as attributes of the page's content (a URL shortener's "Free / No sign-up / QR code included" pills) rather than being visually welded to the headline. No prop changes — only their position in the rendered layout.
